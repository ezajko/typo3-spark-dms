#
# PageTS for spark_dms
# Registers ViewType options for List and Detail plugins
#

@import 'EXT:spark_dms/Configuration/PageTS/ContentElement/Element/Dms.tsconfig'

# Configure default upload folder for DocumentVersion file field
TCEFORM.tx_sparkdms_domain_model_documentversion.file {
    config.uploadFolder = 2:/_inbox
}
